<Window x:Class="SpriteAnimator.Views.CreateAtlasPromptView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Atlas Prompt" Height="150" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <TextBlock Text="{Binding FileName, StringFormat='Unable to find atlas for image {0}.  Create one?'}" Grid.Row="0" />
        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="1" >
            <Button x:Name="Okay" Content="Okay" Margin="10 0" Height="40" Width="125" />
            <Button x:Name="Cancel" Content="Cancel" Margin="10 0" Height="40" Width="125"  />
        </StackPanel>
    </Grid>
</Window>
